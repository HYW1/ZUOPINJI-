# Case · 选择保单改为下拉切换

- **File**: `DLepVQbX1Ll21Y2fh1kmyU`
- **Page frame**: `114:853`（选保单中间页）
- **New**: `288:377` 选择保单-下拉（已挂回手机框内）
- **Old**: `114:861` 列表态（已隐藏并移出画布）
- **Date**: 2026-07-29

## 改法

列表双卡 +「查看更多」→ **下拉切换**：

1. 标题：选择保单  
2. 灰底触发器：当前保单名 + 保障期间 + ∨  
3. 一行保障范围说明（不再用第二张厚卡）  
4. 展开层：两项可选，选中项浅蓝底 + ✓ + 品牌蓝字  

就诊信息模块上移贴合新卡高度；顶区用轻蓝雾矩形替代烘焙截图底，避免旧列表残影。

## 质感对齐（c-end-visual-craft）

- 卡片仅作交互容器（T-005），去掉双厚卡堆叠  
- 行动色 `#1677FF` 只点睛选中态，无大面积假渐变  
- 顶雾溶白（T-009）+ 白岛内容区，无 DROP_SHADOW 抬卡  

## 踩坑

内容层曾被 `appendChild` 到画布根（`0:1`），手机框 `114:853` 只剩隐藏旧层；已全部 reparent 回框内。部分文字误用 `Pingfang SC`（小写 f），移动前需改回 `PingFang SC`。
